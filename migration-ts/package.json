{
  "name": "divergenzmaschine-ts",
  "version": "4.12.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "esbuild test/property.ts --bundle --platform=node --format=cjs --outfile=test/.property.cjs --log-level=error && node test/.property.cjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vite-plugin-singlefile": "^2.0.1"
  },
  "dependencies": {
    "jsdom": "^29.1.1"
  }
}
